[alias]
    fa = fetch --all
    ma = checkout master
    co = checkout
    lb = checkout -
	ctags = !.git/hooks/ctags
    st = status -sb

[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    grep = auto

[core]
	excludesfile = ~/.cvsignore
	pager = less -S

[init]
	templatedir = ~/.git_template

[user]
    name = Thialfihar
    email = thi@thialfiha"r.or"g

[push]
	default = simple
